<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/white_10_transparent</item>
    </style>
    <style name="ActionButtonStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:minWidth">50dip</item>
        <item name="android:maxWidth">250dip</item>
        <item name="android:paddingStart">25dip</item>
        <item name="android:paddingEnd">30dip</item>
    </style>


</resources>
